% Generated by tinyroxygen: do not edit by hand
% Please edit documentation in rbuildignore.R
\name{pkg_copy_filtered}
\alias{pkg_copy_filtered}
\title{Copy package sources while respecting .Rbuildignore}
\usage{
pkg_copy_filtered(pkgdir, dest)
}

\arguments{
\item{pkgdir}{The path to the package directory.}

\item{dest}{The destination directory to copy the filtered sources into.
 Created if it does not already exist.}
}

\value{
Invisibly returns the destination path.
}

\description{
Copy only the files of a package that would NOT be excluded by
 its '.Rbuildignore' file into a destination directory. Directories that
 match an ignore pattern are never recursed into (and so never listed),
 which avoids the cost of walking huge ignored trees (e.g. a 'dev/' folder
 with a JS 'node_modules' install) before handing the source off to
 'R CMD build'/'R CMD check'.
}

\keyword{internal}
